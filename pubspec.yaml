name: lucidlogs
description: "A new Flutter project."

version: 1.0.0+1

environment:
  sdk: ">=3.2.6 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  shared_preferences: ^2.0.15 # Add this line (make sure it's the latest version)
  flutter_colorpicker: ^1.0.3 # Add this line
  syncfusion_flutter_charts: ^20.2.48
  cupertino_icons: ^1.0.2
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.1.3
  provider: ^6.1.2
  intl: ^0.17.0
  google_fonts: ^6.2.1
  flutter_dotenv: ^5.0.2
  http: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  isar_generator: ^3.1.0+1
  build_runner: ^2.4.9

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - .env
